inputs = [1.2,5.1,2.1] #unique inputs
weights = [3.1,2.1,8.7]
bias = 3 #each weight has a bias
#pretty much the output for the neural network for now
output = inputs[0]*weights[0] + inputs[1]*weights[1] + inputs[2]*weights[2] + bias
print(output) 

